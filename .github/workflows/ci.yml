name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Pluto.slnx

      - name: Build
        run: dotnet build Pluto.slnx --no-restore -c Release

      - name: Test
        run: dotnet test Pluto.Tests/Pluto.Tests.csproj --no-build -c Release
